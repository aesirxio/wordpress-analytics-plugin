{
  "name": "aesirx_analytics",
  "version": "1.0.1-alpha.1",
  "description": "analytics via aesirx",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp zip"
  },
  "devDependencies": {
    "del": "^7.0.0",
    "gulp": "^4.0.2",
    "gulp-zip": "^5.1.0"
  },
  "dependencies": {
    "aesirx-analytics": "^1.0.5"
  }
}
